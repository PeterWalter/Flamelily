<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="flamelily.Views.LoginPage">
    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <Label Text="Log-in Page" />
        <Entry Text="{Binding UserName}" Placeholder="Enter User name" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
        <Entry IsPassword="True" Text="{Binding Password}" Placeholder="enter password" />
        <Button Text="Login" Command="{Binding onExecuteCommand}" />
    </StackLayout>
</ContentPage>