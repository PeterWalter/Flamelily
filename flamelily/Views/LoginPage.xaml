<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             Title="{Binding Title}"
             x:Class="flamelily.Views.LoginPage">
    <StackLayout HorizontalOptions="Fill" VerticalOptions="CenterAndExpand">
        
    
    <StackLayout Padding="20" HorizontalOptions="CenterAndExpand"  VerticalOptions="CenterAndExpand">
        <Label Text="Log-in your details" HorizontalOptions="CenterAndExpand" />
    </StackLayout>
    <StackLayout Padding="20" HorizontalOptions="Fill"  VerticalOptions="CenterAndExpand">
    <Entry Text="{Binding UserName}" Placeholder="Enter user name"  VerticalOptions="CenterAndExpand"/>
        <Entry IsPassword="True" Text="{Binding Password}" Placeholder="Enter password" />
            <Button Text="Submit" Command="{Binding OnExecuteCommand}" CommandParameter="ControlPage/NavigationPage/RegistryPage" BackgroundColor="Beige" />
    </StackLayout>
    </StackLayout>
</ContentPage>