<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="sacapMobile.Views.LoginView"
             Visual="Material"
             BindingContext="Binding loginVM, Source={StaticResource Locator}">
    <ContentPage.Content>
        <StackLayout Margin="10">
            <Label Text="Welcome to SACAP!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Entry  Placeholder="Enter email address" 
                    FontSize="Medium" Text="{Binding Email, Mode=TwoWay}" 
                    PlaceholderColor="#F0A9A9" />
            <Entry  Placeholder="password" 
                    IsPassword="True"  
                    FontSize="Medium" 
                    Text="{Binding Password, Mode=TwoWay}"/>
            <Button Text="log-in" 
                    Command="{Binding loginCommand}" BorderColor="#9B9393" BackgroundColor="#122FCF"/>
        </StackLayout>
        

    </ContentPage.Content>
</ContentPage>